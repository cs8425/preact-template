{
    "name": "preact-template",
    "version": "1.0.0",
    "description": "A simple project using Preact.js and esbuild.",
    "dependencies": {
        "preact": "^10.5.14"
    },
    "devDependencies": {
        "esbuild": "^0.14.11",
        "serve-handler": "^6.1.3"
    },
    "scripts": {
        "build": "node esbuild.js",
        "start": "node esbuild-dev.js"
    }
}
